---
phase: 05-robustness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/tracker.py]
autonomous: true
requirements: [ROBUST-01]
must_haves:
  truths:
    - "People can be tracked across frames"
    - "Count remains stable during occlusion"
    - "Entering/leaving detection works"
  artifacts:
    - path: "src/tracker.py"
      provides: "People tracking for occlusion"
      exports: ["PeopleTracker"]
---

<objective>
Handle occlusions by tracking people across video frames using centroid matching.

Purpose: When people partially block each other, tracking maintains consistent counts by matching detections to tracked identities.
Output: PeopleTracker class that integrates with detection pipeline.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-robustness/05-CONTEXT.md
@.planning/phases/05-robustness/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create PeopleTracker class</name>
  <files>src/tracker.py</files>
  <action>
    Create src/tracker.py:
    - class PeopleTracker with:
      - track(detections) -> tracked_ids, count
      - Uses centroid distance matching (IoU or distance threshold)
      - Handles entering/leaving (appear/disappear)
      - Maintains stable IDs across frames
    - Configurable max_distance for matching
    - Configurable max_frames_to_skip for lost tracking
  </action>
  <verify>python -c "from src.tracker import PeopleTracker; t=PeopleTracker(); print('Tracker created')"</verify>
  <done>PeopleTracker class created and importable</done>
</task>

</tasks>

<verification>
- [ ] PeopleTracker can track detections
- [ ] Returns stable IDs across frames
- [ ] Handles entering/leaving
</verification>

<success_criteria>
- Tracker maintains consistent IDs
- Count remains stable during occlusions
- Integrates with existing detection pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/05-robustness/05-SUMMARY.md`
</output>