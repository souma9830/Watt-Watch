---
phase: 05-robustness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/smoothing.py]
autonomous: true
requirements: [ROBUST-03]
must_haves:
  truths:
    - "Count can be smoothed over time"
    - "Rapid fluctuations are reduced"
    - "Smoothed count is returned"
  artifacts:
    - path: "src/smoothing.py"
      provides: "Motion smoothing"
      exports: ["CountSmoother"]
---

<objective>
Handle erratic motion by smoothing detection counts over time.

Purpose: Reduce jitter in people count by applying temporal smoothing, providing stable output.
Output: CountSmoother class for temporal averaging.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-robustness/05-CONTEXT.md
@.planning/phases/05-robustness/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create count smoothing</name>
  <files>src/smoothing.py</files>
  <action>
    Create src/smoothing.py:
    - class CountSmoother:
      - __init__(window_size=10) - rolling window size
      - update(raw_count) -> smoothed_count
      - Uses rolling average of last N counts
      - get_history() -> list of recent counts
    - Exponential smoothing option (alpha parameter)
  </action>
  <verify>python -c "from src.smoothing import CountSmoother; s=CountSmoother(); print(s.update(5))"</verify>
  <done>CountSmoother created</done>
</task>

</tasks>

<verification>
- [ ] Smoothing reduces count variation
- [ ] Returns stable count
- [ ] Configurable window size
</verification>

<success_criteria>
- Temporal smoothing works
- Reduces rapid fluctuations
- Provides stable output
</success_criteria>

<output>
After completion, create `.planning/phases/05-robustness/05-SUMMARY.md`
</output>