---
phase: 05-robustness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/preprocessing.py]
autonomous: true
requirements: [ROBUST-02]
must_haves:
  truths:
    - "Low-light frames can be detected"
    - "Image enhancement improves detection"
    - "Low-light events are logged"
  artifacts:
    - path: "src/preprocessing.py"
      provides: "Low-light enhancement"
      exports: ["enhance_frame", "detect_low_light"]
---

<objective>
Handle low-light conditions by detecting and enhancing dark frames.

Purpose: Improve detection accuracy in dark environments by applying image enhancement and adjusting thresholds.
Output: Preprocessing functions for low-light handling.
</objective>

<execution_context>
@C:/Users/user/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-robustness/05-CONTEXT.md
@.planning/phases/05-robustness/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create low-light preprocessing</name>
  <files>src/preprocessing.py</files>
  <action>
    Create src/preprocessing.py:
    - detect_low_light(frame) -> bool, brightness_score
      - Calculate mean brightness of frame
      - Return True if below threshold (e.g., < 50)
    - enhance_frame(frame) -> enhanced_frame
      - Apply CLAHE (Contrast Limited Adaptive Histogram Equalization)
      - Convert to YUV, apply to Y channel, convert back
    - LowLightDetector class that tracks low-light events
  </action>
  <verify>python -c "from src.preprocessing import detect_low_light, enhance_frame; print('Preprocessing OK')"</verify>
  <done>Low-light preprocessing created</done>
</task>

</tasks>

<verification>
- [ ] Low-light detection works
- [ ] Enhancement improves visibility
- [ ] Events can be logged
</verification>

<success_criteria>
- Detect low-light conditions
- Apply enhancement that improves detection
- Log low-light events
</success_criteria>

<output>
After completion, create `.planning/phases/05-robustness/05-SUMMARY.md`
</output>